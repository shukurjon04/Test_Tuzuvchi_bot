services:
  quiz-bot:
    build: .
    container_name: quiz-bot
    restart: unless-stopped
    env_file:
      - .env
    dns:
      - 8.8.8.8
      - 8.8.4.4
    volumes:
      - ./tests.txt:/app/tests.txt:ro
